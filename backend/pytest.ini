[pytest]
markers =
    unit: tests without network (DB + pure logic)
    api:  tests that require the backend running on :8000
    integration: tests that require Anvil on :8545 (and optionally the backend)
