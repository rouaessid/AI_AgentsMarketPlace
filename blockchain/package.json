{
  "name": "agentmarket-blockchain",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "anvil --state hardhat-state.json --chain-id 31337 --port 8545",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.28.6",
    "pg": "^8.21.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2"
  }
}
